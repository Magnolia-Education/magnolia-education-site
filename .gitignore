.claude/
.DS_Store
Thumbs.db
*.log
node_modules/

# Next.js build output (generated by Netlify build)
tools/
tools-app/.next/
tools-app/out/

# Netlify CLI link state — machine-local, not the deploy config (that is netlify.toml).
.netlify/

# Local git worktrees. Committing these adds embedded repos that clones cannot resolve.
.worktrees/
